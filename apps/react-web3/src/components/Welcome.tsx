const Welcome = () => {
    const connectWallet = () => {

    }

    return (
        <div>
            <button  onClick={connectWallet}>Connect Wallet</button>
        </div>
    )
}

export default Welcome;
